# spec.md — WordArt 功能与技术规格

> 状态: 初稿(待人工修订) · 写于 2026-08-28(周五)
> 详细技术实现在单独会话中进行,本文只锁定范围与验收口径。

## 产品概述

纯前端 Web 工具,四步完成一次创作:上传图片 → 输入自定义文字 → 生成字符画 → 导出 PNG/PDF 打印。全程无后端、无账号、无上传。

## 对标与差异

同类工具 charart.odin-lab.com 已跑通"图片 + 文字 + 参数面板"的完整交互,验证了品类需求,但未开源,且止步于 PNG 下载。参数面板清单(F4)直接对标它;本项目差异在三条:PDF 打印链路(打印即礼物)、自定义短语平铺的礼物叙事、图片全程本地处理的隐私承诺。

## 功能需求

### P0 — MVP,周六必须交付

| 编号 | 功能 | 要点 |
|---|---|---|
| F1 | 图片上传 | 点击或拖拽;移动端可从相册选图 |
| F2 | 字符画生成 | 图片亮度映射为字符密度/字号;提供 ASCII 字符集与自定义文字两种模式 |
| F3 | 自定义文字 | 用户输入任意短语(如"某某我爱你"),该短语作为画笔平铺整幅图 |
| F4 | 参数调节 | 列数(分辨率)、对比度、亮度、字号、黑字白底/白字黑底 |
| F5 | PDF 导出 | 单页 A4、合理页边距、等宽字体,打印即成品 |
| F6 | 部署 | Cloudflare Pages 免费静态托管,生成 pages.dev 域名 |

### P1 — 周六晚间视进度取舍

| 编号 | 功能 | 要点 |
|---|---|---|
| F7 | 预设字符集 | 数字、代码风、常用中文词,一键切换 |
| F8 | 复制纯文本 | 按行复制,供用户手写誊抄 |
| F9 | 互动模式 | 换词后实时重渲染,响应时间 1 秒内,支撑周日现场互动 |

### P2 — 范围外,只进路线图

- 描红模板导出(镂空字版,配合凹刻板或半透明纸)。
- 多页长图、拼贴、批量生成。

## 非功能需求

- 隐私:图片只进浏览器内存与 Canvas,网络面板零上传请求;页面上明示"图片不会上传"。
- 性能:1080p 图片生成 3 秒内;互动重渲染 1 秒内。
- 兼容:Chrome、Edge、Safari 最新版;手机浏览器完成全流程。
- 成本:静态托管,运营成本为零。
- 兜底:断网状态下页面仍可用(纯静态资源)。

## 技术方案草案

形态为单页静态应用,可选轻量构建(Vite)或零构建。渲染基于 Canvas 2D 读像素亮度;输出排版用等宽字体,保证屏幕预览与 PDF 一致。PDF 生成优先 **jsPDF** 直出文件,备选方案为打印样式表 + `window.print()`。托管用 **Cloudflare Pages**,Git 直连或 wrangler CLI 部署。首页案例成品图属静态资源,托管在**腾讯云 COS** 走 CDN 直链;用户上传的图片不受影响,仍在浏览器本地处理,隐私承诺不变。

具体选型在实现会话中定稿,以下决策原则先固定:

| 决策点 | 倾向 | 理由 |
|---|---|---|
| 纯前端 vs 后端 | 纯前端 | 隐私是卖点,零成本,一天可交付 |
| 密度表现 | 字号调制为主,密度为辅 | 短语重复平铺时,字号调制稳定且文字可读 |
| 算法来源 | 双引擎 | 浏览器端自写轻量采样循环(几十行);案例批量生成走 Python 管线:wordcloud 出"姓名拼图"PNG,ascii-magic 出纯文本版(梯度字符 translate 成短语),成熟库保证鲁棒性 |
| PDF 方案 | jsPDF 优先 | 直接生成文件、离线可用;print 方案作退路 |
| 案例图托管 | 腾讯云 COS | 案例属静态资源,走 CDN 直链;用户图片仍全在本地,隐私卖点不破 |
| 部署 | Cloudflare Pages | 免费、自带域名、部署耗时以分钟计 |

## 验收口径(周六)

- [ ] 手机照片上传后 10 秒内看到字符画预览
- [ ] 输入自定义短语后,画面构成随之变化
- [ ] 导出 PDF 并打印 1 张,3 米外可辨认出原图
- [ ] PNG 双倍(2x)导出可用,分享场景不糊
- [ ] 手机浏览器打开线上地址跑通全流程
- [ ] 断网后页面仍可正常使用
