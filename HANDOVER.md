# HANDOVER.md — WordArt 开发交接

> 写于 2026-08-28(周五)晚 · 供新开发会话使用
> 硬背景:黑客松 2026-08-29(周六)开发,08-30(周日)下午 5 分钟展示,时间极紧,周六 22:30 前须全部就绪

## 一分钟了解项目

WordArt:用户上传图片 + 输入自定义文字(如"某某我爱你"),生成由该文字拼成的字符画,导出 PNG/PDF 打印,做成可送人的礼物。核心卖点:打印即礼物、文字自带纪念意义、图片全程本地处理。

对标:WordCloud 按词频摆位置,本项目按图片明暗排文字;竞品 charart.odin-lab.com 验证了品类需求,但止步于 PNG 下载,打印链路与礼物叙事是本项目差异。

## 仓库现状

- 路径:`D:\202609_Exercise\1_hackathon\WordArt`
- Git:`main` 分支已初始化,**尚无任何提交**,开工前先做首次 commit
- 现有文件:`intent.md`、`spec.md`、`plan.md`、`.gitignore`、本文件,无任何代码

## 必读文档(按此顺序)

1. `spec.md` — 功能分级(F0/P0 到 P2)、技术决策表、验收标准,开发的直接依据
2. `intent.md` — 立项动机、用户场景(恋爱合影、全员姓名拼吉祥物),讲故事的素材
3. `plan.md` — 周六逐时段时间表、里程碑、展示脚本、风险兜底

## 已锁定的关键决策

1. **无后端服务器**。用户说的"实现后端效果"指核心渲染引擎,不是搭服务。用户图片只在浏览器本地处理,网络面板零上传请求——这是展示时要讲的隐私卖点,不能破。
2. **双引擎架构**:
   - Python 本地管线(离线脚本,非服务):周六下午批量生成案例。`wordcloud` 出"姓名拼图"PNG 大图(PPT 主打,中文字体必须传 `font_path` 否则全是方框);`ascii-magic` 出纯文本版,其固定梯度字符用 `str.translate()` 逐个映射成用户短语,空格保留
   - JS 浏览器端(线上):Canvas 采样循环,几十行量级,支撑现场互动
3. **部署**:Cloudflare Pages 静态托管,免费,自带 pages.dev 域名;案例成品图(静态资源)托管腾讯云 COS 走 CDN 直链
4. **PDF**:jsPDF 优先直出文件,打印样式表 + `window.print()` 作退路
5. **参数面板语义照抄竞品**:渲染模式(原色/单色)、背景颜色、输出宽度、字号大小、字间距、亮度阈值、反转前景/背景、加粗边缘;竞品的"亮度阈值 + 加粗边缘"说明走阈值渲染路线,实现比字号调制简单,推荐照此建模
6. **导出**:PNG 1x + 2x 高清与 PDF 并存

## 开发顺序(周六,来自 plan.md)

| 时段 | 任务 |
|---|---|
| 09:00–12:00 | 核心引擎:亮度矩阵、自定义文字、参数面板 |
| 13:00–15:00 | 调试 + PDF 导出,15:00 硬停调参 |
| 15:00–17:00 | 案例批量生成(Python 管线):合影、logo、梗图、吉祥物方向 6–8 张 |
| 17:00–19:00 | 前端构建:界面美化、案例画廊,案例图上传 COS |
| 19:00–20:00 | 部署 Cloudflare Pages,手机实测 |
| 20:00–22:30 | PPT 初稿、引例与笑点、录屏、打印实物 2–3 张 |

新会话先实现核心引擎跑出效果,再搭前端展示,与上表一致。

## 验收标准(周六晚自查)

- 手机照片上传后 10 秒内看到预览
- 输入自定义短语后画面构成随之变化
- 导出 PDF 打印 1 张,3 米外可辨认原图
- PNG 2x 导出不糊
- 手机浏览器打开线上地址跑通全流程
- 断网后页面仍可用(纯静态资源)
- 隐私:网络面板无任何图片上传请求

## 风险红线

- 周六 15:00 硬停调参,之后只修不加
- 前端美化限时 2 小时,用现成模板/组件库,到点即收
- 不搭服务器、不上传用户图片、不做账号系统
- 中文渲染:wordcloud 必须传 `font_path`;浏览器 Canvas 需指定 CJK 字体
- 周日只做 PPT 定稿、排练 2 遍、兜底,不写新功能

## 赛后衔接

- 09-01(周二)进阶培训:赛事总结、Git 工程化,顺手整理仓库为规范提交
- 正赛提交截止 09-30,作品需与工作或营销业务相关,可向"内容营销物料生成"延伸
- 5 次培训作业,1 个月内完成,Ruby 飞书链接收集,勿拖到月末

## 给新会话的开场建议

> 请先读 `HANDOVER.md`,再按顺序读 `spec.md`、`intent.md`、`plan.md`,然后开始实现核心渲染引擎。注意:无后端服务器,用户图片只在浏览器本地处理。
